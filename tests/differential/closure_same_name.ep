define first_maker returning Int:
    set f to given x:
        return x + 1
    return f(10)

define second_maker returning Int:
    set f to given x:
        return x + 2
    return f(10)

define main:
    display first_maker()
    display second_maker()
    return 0
