define choice Box:
    variant Full with v as Int
    variant Empty

define main:
    set a to Full with 5
    set b to Empty
    check a:
        if Full with x:
            check b:
                if Full with y:
                    display x + y
                if Empty:
                    display x
        if Empty:
            display 0
    return 0
